<p>ここは一覧ページです。</p>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.id, task %>：<%= task.status %>&gt;<%= task.content %></li>
  <% end %>
</ul>

<%= link_to '新規タスクの投稿', new_task_path, class: 'btn btn-lg btn-primary'%>
<%= link_to 'Sign up now', signup_path, class: 'btn btn-lg btn-primary' %>
<%= paginate @tasks %>